---
phase: 15-mastery-curriculum
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [arcproLab/scripts/train_policy.py, arcproLab/policy_stack/policies/fusion_policy.py, arcproLab/policy_stack/wrappers/curriculum_callback.py]
autonomous: true
requirements: [REQ-MASTERY-LAP, REQ-MASTERY-RAM, REQ-MASTERY-MONITOR]

must_haves:
  truths:
    - "SB3 RolloutBuffer uses uint8 for images (reduced RAM usage)"
    - "Tensorboard logs 'curriculum/boundary_margin' and 'curriculum/max_episode_length'"
    - "Boundary threshold in env decreases linearly from 0.25 to 0.13 over 1M steps"
  artifacts:
    - path: "arcproLab/policy_stack/wrappers/curriculum_callback.py"
      provides: "Dynamic boundary scheduler and logging"
    - path: "arcproLab/scripts/train_policy.py"
      provides: "uint8 observation space configuration"
  key_links:
    - from: "arcproLab/policy_stack/wrappers/curriculum_callback.py"
      to: "tensorboard"
      via: "self.logger.record"
---

<objective>
Optimize training infrastructure for production scale and implement the mastery curriculum scheduler. This plan reduces RAM footprint by 4x using uint8 image storage and adds automated monitoring of curriculum progress and episode length.

Purpose: Enable high-resolution training with more parallel environments on limited RAM.
Output: uint8 optimized training pipeline and curriculum monitoring callback.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@arcproLab/scripts/train_policy.py
@arcproLab/policy_stack/policies/fusion_policy.py
@.planning/phases/15-mastery-curriculum/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: uint8 RAM Optimization</name>
  <files>arcproLab/scripts/train_policy.py, arcproLab/policy_stack/policies/fusion_policy.py</files>
  <action>
    - In `arcproLab/scripts/train_policy.py`:
      - Update `HPPPDirectBridge.__init__` to define `image` observation space as `dtype=np.uint8` with range `[0, 255]`.
      - Update `HPPPDirectBridge._process_obs` to convert image to `uint8` (scale 0-1 float to 0-255 uint8) using `(img * 255).astype(np.uint8)`.
    - In `arcproLab/policy_stack/policies/fusion_policy.py`:
      - In `FusionFeaturesExtractor.forward`, ensure `observation["image"]` is cast to float and normalized: `img = observation["image"].float() / 255.0`.
    - This ensures the SB3 `RolloutBuffer` stores `uint8` (1 byte per pixel instead of 4), enabling 12+ environments on 32GB RAM.
  </action>
  <verify>
    <automated>python3 -c "import numpy as np; from arcproLab.scripts.train_policy import HPPPDirectBridge; import gymnasium as gym; print(HPPPDirectBridge.__init__)" (Manual check of code for uint8 cast)</automated>
  </verify>
  <done>Observation space is uint8 and features extractor correctly normalizes it.</done>
</task>

<task type="auto">
  <name>Task 2: Curriculum & Monitoring Callback</name>
  <files>arcproLab/policy_stack/wrappers/curriculum_callback.py, arcproLab/scripts/train_policy.py</files>
  <action>
    - Create `arcproLab/policy_stack/wrappers/curriculum_callback.py` inheriting from SB3 `BaseCallback`.
    - Implement `_on_step`:
      - Track `max_ep_len` across all envs using `infos`.
      - Log `max_ep_len` to tensorboard as `curriculum/max_episode_length`.
      - Implement linear boundary scheduler: `current_margin = max(0.13, 0.25 - (self.num_timesteps / 1_000_000) * (0.25 - 0.13))`.
      - Update the environment boundary threshold: `self.training_env.set_attr("roadmark_contact_threshold", current_margin)` or directly modify the cfg if possible via `env_method`.
      - Log `current_margin` to tensorboard as `curriculum/boundary_margin`.
    - In `arcproLab/scripts/train_policy.py`:
      - Import and initialize `CurriculumCallback`.
      - Add it to the list of callbacks in `model.learn`.
    - Update `HPPPDirectBridge` to support setting the threshold if needed.
  </action>
  <verify>
    <automated>ls arcproLab/policy_stack/wrappers/curriculum_callback.py</automated>
  </verify>
  <done>Curriculum callback is implemented, integrated, and logging to Tensorboard.</done>
</task>

</tasks>

<verification>
Run a short training session (1000 steps) and verify `logs/ppo/*/tb` contains the new curriculum metrics.
</verification>

<success_criteria>
- SB3 buffer memory usage is significantly reduced (verified via `htop` or logs).
- Tensorboard shows decreasing `boundary_margin` and increasing `max_episode_length`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-mastery-curriculum/15-02-SUMMARY.md`
</output>
