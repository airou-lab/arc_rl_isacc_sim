# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | reinforcement-learning | How to handle stagnation at curves caused by waypoint tangent target rewards. | Removed waypoint track rewards in favor of purely visual lateral error and heading rewards. Update speed_reward to just reward local forward velocity (+X). | The ResNet policy was being artificially penalized on curves because waypoint-based rewards wanted straight lines. Detaching reward from waypoints forces the vision backbone to learn the lane naturally without being hampered by geometry constraints. | Yes | agent |
| D002 |  | physics | How to align the coordinate frame for steering and lateral error. | Set Target Offset to 0.0m, Steering to Positive=Left, and Lateral Error to Positive=Left. | Previous models experienced the "death swerve" because steering and lateral error were inverted, and target offset (-0.238m) pushed the robot into the right-hand wall on narrow lanes. Standardizing to a consistent Right-Handed Frame directly solves this bias. | Yes | agent |
| D003 |  | physics | How to synchronize Isaac Lab physics with the real-world F1Tenth model. | Chassis mass override set to 3.342kg so total mass equals 4.092kg. effort_limit_sim reduced to 0.5. drive_scale remains at -40.0. | Properly matching the real-world 4.092kg mass and dialing down motor torque stops the robot from "kangaroo hopping" due to overpowered motors acting on mismatched mass. Maintaining drive_scale at -40.0 correctly moves the robot forward relative to the straightaway. | Yes | agent |
| D004 |  | architecture | How to configure PPO exploration for narrow-track continuous control. | Set ent_coef=0.0 and initialize log_std_init=-0.5 in policy_kwargs for RecurrentPPO. | Standard high-variance exploration (std=1.0) causes instant boundary collisions, preventing the vision backbone from seeing valid trajectories. Clipped high-variance actions also warp PPO probability ratios, causing catastrophic KL divergence spikes. Tightly bounding the initial action distribution prevents this. | Yes | agent |
| D005 |  | reinforcement-learning | How to break out of the 0.28 m/s crawling local minimum caused by reward bloat. | Implemented Curriculum Phase 1: Disabled stationary, jerk, smoothness, and boundary penalties. Replaced linear speed reward with a strict Quadratic Target Velocity (centered at 2.0 m/s). Completely disabled the -200 termination penalty to remove the fear of death. | The agent was mathematically optimizing for survival by driving slowly to delay the massive -200 death penalty. Removing all penalties forces it to solely optimize the quadratic velocity target. Once it learns to sprint, penalties will be reintroduced in Phase 2. | Yes | agent |
| D006 |  | computer-vision | How to configure the TiledCamera to enable high-speed anticipation. | Removed the 30-degree downward tilt on the camera offset, returning it to an identity rotation `(1.0, 0.0, 0.0, 0.0)`. Implemented `fov_visibility_termination` to enforce track visibility. | The 30-degree tilt forced the camera to stare directly at the ground in front of the bumper, causing shortsightedness. At 2.0 m/s, the agent needs to see the horizon to anticipate curves. | Yes | agent |
| D007 |  | reinforcement-learning | Unfroze the entire ResNet-18 backbone in `fusion_policy.py`. | Unfroze backbone. Re-enabled `termination_penalty` (-50). | Relying on frozen ImageNet filters for a synthetic track was likely bottlenecking feature extraction. Re-enabling termination penalty penalizes crashing without inducing 'fear of moving'. | Yes | agent |
| D008 |  | physics | Correctly synced camera orientation, spawn orientation, and drive direction. | Restored 180-deg camera rotation (`rot=(1.0,0,0,0)`), spawn `yaw=1.5708`, and drive `scale=-40.0`. Disabled `driving_blind` termination. | The USD's native camera is rigged backwards. Removing the 180-deg compensation caused the camera to face backwards relative to the chassis. Driving backwards (South) down the North-facing track properly aligns the camera's forward view with the car's direction of travel. Disabling `driving_blind` prevents immediate termination from the 180-deg heading mismatch. | Yes | agent |
| D009 | | reinforcement-learning | Fixed 5 critical RL bugs | Fixed Suicide Bias, 3D Boundary Bug, Distance Math, Double Stepping, 1-Step Reward Delay | Ensures mathematical correctness of the RL loop prior to vision training. | Yes | agent |
| D010 | | computer-vision | (REVERTED) Added forward waypoints to telemetry | Injected 5 waypoints and completely froze CNN. | Reverted because providing numerical anticipation bypassed the requirement (R002) for the agent to learn HD Vision-based navigation. | Yes | agent |
| D011 | | computer-vision | (SUPERSEDED) Implemented Balanced Transfer Learning | Froze layers 1-3, unfroze layer 4. | Unfreezing the whole CNN caused PPO explosions. Freezing 1-3 kept basic edge detectors. Reverted because normalization was actually the root cause of blindness. | Yes | agent |
| D012 | | computer-vision | Fixed CNN Normalization and Fully Froze CNN | Removed conditional check bypassing ImageNet normalizations. Completely froze ResNet-18 backbone. | The custom 'Mastery Fix' was blocking the environment's [0, 1] images from being normalized with ImageNet stats, causing the frozen layers to output garbage. Now that the images are correctly normalized, the full pretrained backbone can be used safely without RL gradients. | Yes | agent |
| D013 | | physics | Fixed Zoolander Bug and Suicide Bias | Renamed ActionCfg terms to 'a_steering' and 'b_drive'. Reduced stationary penalty to 0.05. | IsaacLab alphabetizes config terms. 'drive' came before 'steering', shifting steering to index 2 where it was clipped by the 0.0 to 1.0 brake bounds, preventing right turns. Reducing stationary penalty prevents the agent from intentionally crashing to avoid stagnation bleed. | Yes | agent |
| D014 | | reinforcement-learning | Fixed Reward Farming Exploit | Rewrote `progress_reward` to be linearly proportional to forward speed, and `lateral_error_reward` to be a pure penalty. | The agent refused to learn to turn by 2M steps, opting to drive slowly straight into the wall at 0.37 m/s. Discovered that the Quadratic Velocity reward and Centering reward granted massive positive step-rewards just for existing. The slower it drove, the more steps it stayed alive, accumulating more reward before the crash penalty. Making reward equal to speed means Total Reward = Distance Traveled, completely nullifying the time exploit. | Yes | agent |
| D015 | | reinforcement-learning | Fixed Playing Dead Trap | Disabled `termination_penalty` entirely (weight 0.0). | The agent was so terrified of the -200 wall crash penalty that it refused to move. Since the reward is now purely distance-based, crashing naturally stops point accumulation. Explicit crash penalties create a fear-barrier that suppresses exploration. | Yes | agent |
| D016 | | infrastructure | Hardened debug tools and watchdog | Restored `debug.sh` and patched `verify_policy.py` for nested `"policy"` observation structure. Hardened `watchdog.py`. | Replaced memory-heavy subagents with lightweight daemon-based Antigravity summons for milestone reporting and autonomous repair. | Yes | agent |
| D017 | | architecture | Migrated to SKRL and ResNet-18 | Replaced Stable-Baselines3 with SKRL. Implemented a frozen `ResNet-18` inside `SKRLFlattenWrapper` to cache camera features (524 dims). Restarted training from Step 0. | SKRL is natively built for IsaacLab and scales better. Evaluating the camera image once during rollout via a frozen ResNet-18 and caching it in the Replay Buffer made PPO lightning fast compared to processing raw images inside the policy network. | Yes | agent |
| D018 | | reinforcement-learning | Re-implemented Phase 1 Curriculum for ResNet Agent | Zeroed out strict Phase 2 precision penalties (`lateral_error`, `stationary`, `heading`, `smoothness`, `jerk`). Kept `survival_bonus` and `progress_reward`. | The new ResNet brain was experiencing 'immediate termination' because it was being hit with strict Phase 2 penalties (like the `stationary` penalty demanding speeds >0.5m/s) before it had even learned basic forward momentum mapping for its new visual features. | Yes | agent |
| D019 | | reinforcement-learning | Fixed Endless Runner exploit in Phase 1 | Increased `termination_penalty` weight from `20.0` to `100.0` (-5000 penalty) and halved `progress_reward` weight from `50.0` to `25.0`. | Without precision penalties, the agent reverted to drag-racing and crashing (169 len, 0.31m/s) because the high progress reward outpaced the crash penalty. Crashing must mathematically wipe out the accumulated rewards of a short sprint to force the agent to brake and corner. | Yes | subagent |
| D020 | | reinforcement-learning | Fixed Playing Dead Trap via Bounded Rewards | Applied `torch.tanh` to `progress_reward` to cap it, reduced `termination_penalty` back to 20.0, and lowered `survival_bonus` to 1.0. | The -5000 crash penalty from D019 induced a 'Playing Dead Trap' where the agent refused to move. Bounding the speed reward via tanh fundamentally solves the Endless Runner exploit without requiring massive crash penalties, allowing for stable, fearless exploration. | Yes | subagent |
| D021 | | reinforcement-learning | Re-enabled Stationary Penalty | Re-enabled `stationary` penalty with a moderate weight of `5.0`. | Bounding the progress reward and lowering the survival bonus caused the agent to become overly conservative again (0.22 m/s). The moderate stationary penalty yields a net -4.0 penalty per step if the agent drives under 0.5 m/s, forcing it to break out of the conservative local minimum and speed up. | Yes | subagent |
| D022 | | reinforcement-learning | **ROOT CAUSE FIX**: Replaced `progress_reward` with `waypoint_progress_reward` | Deprecated `progress_reward` (local body velocity). Created `waypoint_progress_reward` in `rewards.py` using TrackManager waypoint index deltas. Added `Trk%` and `WPΔ` telemetry to verify real track navigation. | After adding track progress telemetry, discovered the agent was NEVER navigating the track — `WPΔ: 0` even during 1000-step episodes. The old `progress_reward` based on `root_lin_vel_b[:, 0]` was fundamentally exploitable by circling/wiggling near spawn. Waypoint-based progress is the ONLY ungameable metric. | No | agent |
| D023 | 2026-08-16 | architecture | **ROOT CAUSE FIX**: Observation Feedback Poisoning & Policy Saturation | Clamped `obs[:, 5:7]` previous actions in `observations.py` to `[-1.0, 1.0]`. Bound `ARCProActor` output with `torch.tanh`. Removed brittle watchdog kill switches. | Discovered that un-clamped raw policy outputs (e.g. +13, -9) were fed directly back into `obs[:, 5:7]`, causing an observation drift feedback loop where the network's deterministic mean saturated into full brake lock (-9.5 ➔ -1.0). In training, Gaussian exploration noise masked this by occasionally sampling positive throttle, but eval mode exposed 0.0 m/s brake lock. Clamping observation inputs and bounding the actor head guarantees stable, forward-driving policy convergence. | No | agent |
| D024 | 2026-08-17 | reinforcement-learning | **ROOT CAUSE FIX**: Crawling Survival Trap & Action Drive Restoration (Issue 86) | Set `action_drive_reward` weight to `20.0` in `arcpro_env_cfg.py` and tightened `stagnation_termination` to 100 steps in `terminations.py`. | When `action_drive_reward` was 0.0 and stagnation allowed 300 steps, the agent learned that creeping at 0.04 m/s delayed line collision for 250+ steps, accumulating higher expected return than fast driving. Restoring forward drive incentive and tightening stagnation destroys the crawling exploit. | No | agent |
| D025 | 2026-08-17 | reinforcement-learning | **ROOT CAUSE FIX**: Dense Centerline Guidance Restoration (Issue 87) | Enabled `lateral_error` in `arcpro_env_cfg.py` with weight `1.0` (`-(abs_lat * 10.0)`). | With both `lateral_error` and `boundary_penalty` disabled, the agent faced a binary cliff death (-500 on contact) with zero intermediate distance feedback. Providing a dense lateral penalty directly guides the ResNet-18 visual perception toward centerline waypoints well before hitting the edge lines. | No | agent |
| D026 | 2026-08-19 | reinforcement-learning | Straightaway Stability Tuning (Issue 88) | Increased `lateral_error` to 2.5 and enabled `jerk_penalty` at 0.05. | The agent was dying on straightaways due to micro-weaving (no jerk penalty) and edge-hugging (heading reward overpowered the weak lateral penalty). Higher lateral error forces strict centering, and jerk penalty forces smooth steering arcs, stabilizing high-speed straightaways. | Yes | agent |
| D027 | 2026-08-26 | reinforcement-learning | Silent Jerk Penalty No-Op Bug & Steering Oscillation Fix (Issue 89) | Wired active `prev_action` tracking and reset clearing in `mdp/rewards.py` and `mdp/events.py`. Scaled `jerk_penalty` weight to `0.5`, reset steering `offset = 0.0`, and boosted `lateral_error` weight to `10.0`. | `jerk_penalty` previously returned 0.0 on every step because `prev_action` was never populated in `env.extras`. This caused bang-bang steering oscillations (+1.25 -> -1.74) during high-speed rollouts. Activating `prev_action` tracking and jerk penalty directly penalizes violent steering snaps and enforces smooth arcs. | Yes | agent |


