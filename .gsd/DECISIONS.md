# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | reinforcement-learning | How to handle stagnation at curves caused by waypoint tangent target rewards. | Removed waypoint track rewards in favor of purely visual lateral error and heading rewards. Update speed_reward to just reward local forward velocity (+X). | The ResNet policy was being artificially penalized on curves because waypoint-based rewards wanted straight lines. Detaching reward from waypoints forces the vision backbone to learn the lane naturally without being hampered by geometry constraints. | Yes | agent |
| D002 |  | physics | How to align the coordinate frame for steering and lateral error. | Set Target Offset to 0.0m, Steering to Positive=Left, and Lateral Error to Positive=Left. | Previous models experienced the "death swerve" because steering and lateral error were inverted, and target offset (-0.238m) pushed the robot into the right-hand wall on narrow lanes. Standardizing to a consistent Right-Handed Frame directly solves this bias. | Yes | agent |
| D003 |  | physics | How to synchronize Isaac Lab physics with the real-world F1Tenth model. | Chassis mass override set to 3.342kg so total mass equals 4.092kg. effort_limit_sim reduced to 0.5. drive_scale remains at -40.0. | Properly matching the real-world 4.092kg mass and dialing down motor torque stops the robot from "kangaroo hopping" due to overpowered motors acting on mismatched mass. Maintaining drive_scale at -40.0 correctly moves the robot forward relative to the straightaway. | Yes | agent |
| D004 |  | architecture | How to configure PPO exploration for narrow-track continuous control. | Set ent_coef=0.0 and initialize log_std_init=-0.5 in policy_kwargs for RecurrentPPO. | Standard high-variance exploration (std=1.0) causes instant boundary collisions, preventing the vision backbone from seeing valid trajectories. Clipped high-variance actions also warp PPO probability ratios, causing catastrophic KL divergence spikes. Tightly bounding the initial action distribution prevents this. | Yes | agent |
| D005 |  | reinforcement-learning | How to break out of the 0.28 m/s crawling local minimum caused by reward bloat. | Implemented Curriculum Phase 1: Disabled stationary, jerk, smoothness, and boundary penalties. Replaced linear speed reward with a strict Quadratic Target Velocity (centered at 2.0 m/s). Completely disabled the -200 termination penalty to remove the fear of death. | The agent was mathematically optimizing for survival by driving slowly to delay the massive -200 death penalty. Removing all penalties forces it to solely optimize the quadratic velocity target. Once it learns to sprint, penalties will be reintroduced in Phase 2. | Yes | agent |
| D006 |  | computer-vision | How to configure the TiledCamera to enable high-speed anticipation. | Removed the 30-degree downward tilt on the camera offset, returning it to an identity rotation `(1.0, 0.0, 0.0, 0.0)`. Implemented `fov_visibility_termination` to enforce track visibility. | The 30-degree tilt forced the camera to stare directly at the ground in front of the bumper, causing shortsightedness. At 2.0 m/s, the agent needs to see the horizon to anticipate curves. | Yes | agent |
| D007 |  | reinforcement-learning | Unfroze the entire ResNet-18 backbone in `fusion_policy.py`. | Unfroze backbone. Re-enabled `termination_penalty` (-50). | Relying on frozen ImageNet filters for a synthetic track was likely bottlenecking feature extraction. Re-enabling termination penalty penalizes crashing without inducing 'fear of moving'. | Yes | agent |
| D008 |  | physics | Correctly synced camera orientation, spawn orientation, and drive direction. | Restored 180-deg camera rotation (`rot=(1.0,0,0,0)`), spawn `yaw=1.5708`, and drive `scale=-40.0`. Disabled `driving_blind` termination. | The USD's native camera is rigged backwards. Removing the 180-deg compensation caused the camera to face backwards relative to the chassis. Driving backwards (South) down the North-facing track properly aligns the camera's forward view with the car's direction of travel. Disabling `driving_blind` prevents immediate termination from the 180-deg heading mismatch. | Yes | agent |
