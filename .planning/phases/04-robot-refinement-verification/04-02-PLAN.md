# Plan 2: Verification & Debug Tools
**File:** `.planning/phases/04-robot-refinement-verification/04-02-PLAN.md`

```markdown
---
phase: 04-robot-refinement-verification
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [arcproLab/arcpro_env_cfg.py, arcproLab/scripts/verify_robot.py]
autonomous: true
requirements: [4.3, 4.4]
must_haves:
  truths:
    - "Viewer allows free fly-through navigation tracking the robot"
    - "Verification script reports mass, dimensions, and sensor offsets"
  artifacts:
    - path: "arcproLab/scripts/verify_robot.py"
      provides: "Physical audit capability"
---

<objective>
Implement debugging tools including a free camera for visual inspection and a deep verification script to audit the physics of the refined robot.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Viewer Tracking Configuration</name>
  <files>arcproLab/arcpro_env_cfg.py</files>
  <action>
    - Configure `ViewerCfg` in `ARCProEnvCfg`.
    - Set `origin_type="asset_root"` to track the robot.
    - Ensure `asset_name="robot"`.
    - Set initial eye/lookat coordinates for a good starting view.
  </action>
  <verify>Run environment and verify viewer tracks robot while allowing manual flight.</verify>
  <done>Debug viewer is operational.</done>
</task>

<task type="auto">
  <name>Task 2: Deep Verification Script</name>
  <files>arcproLab/scripts/verify_robot.py</files>
  <action>
    - Create a standalone Isaac Lab script.
    - Implement mass audit: Sum `robot.data.default_mass`.
    - Implement dimension audit: Calculate Bounding Box using `UsdGeom.BBoxCache`.
    - Implement kinematic audit: Verify Wheelbase (25cm) and Track Width (24cm) from wheel prim locations.
    - Implement sensor audit: Check camera transform relative to chassis root.
  </action>
  <verify>Run `./isaaclab.sh -p arcproLab/scripts/verify_robot.py` and check output.</verify>
  <done>Automated physical audit script created.</done>
</task>

</tasks>

<success_criteria>
- Verification script confirms: Mass 4.092kg (+-10g), Wheelbase 0.25m (+-1mm).
- Camera offset visually and programmatically verified.
</success_criteria>
```
