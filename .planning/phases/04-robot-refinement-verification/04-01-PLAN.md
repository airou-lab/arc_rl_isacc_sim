# Plan 1: Robot Physical Refinement
**File:** `.planning/phases/04-robot-refinement-verification/04-01-PLAN.md`

```markdown
---
phase: 04-robot-refinement-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [arcproLab/arcpro_robot_cfg.py, arcproLab/arcpro_env_cfg.py]
autonomous: true
requirements: [4.1, 4.2]
must_haves:
  truths:
    - "Robot uses F1Tenth_Modern.usd as base asset"
    - "Robot mass is set to exactly 4.092kg"
    - "Camera is positioned at [0.145, 0.0, 0.145] relative to rear axle"
  artifacts:
    - path: "arcproLab/arcpro_robot_cfg.py"
      provides: "Mass and Scale overrides"
    - path: "arcproLab/arcpro_env_cfg.py"
      provides: "Sensor offset and RGB config"
  key_links:
    - from: "arcproLab/arcpro_robot_cfg.py"
      to: "f1tenth_trainer/assets/F1Tenth_Modern.usd"
      via: "usd_path"
---

<objective>
Refine the physical properties of the F1Tenth robot to match real-world curb weight and dimensions, and configure the sensor suite for RGB-only operation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Physical Metric Overrides</name>
  <files>arcproLab/arcpro_robot_cfg.py</files>
  <action>
    - Update `ARCPRO_ROBOT_CFG.spawn.usd_path` to "f1tenth_trainer/assets/F1Tenth_Modern.usd".
    - Add `mass_props=sim_utils.MassPropertiesCfg(mass=4.092)` to the `UsdFileCfg`.
    - Configure `RigidBodyPropertiesCfg` to ensure consistent physics.
    - Set `scale` if necessary to reach 25cm wheelbase (0.25m).
  </action>
  <verify>Check arcproLab/arcpro_robot_cfg.py for mass=4.092 and usd_path.</verify>
  <done>Robot config updated with correct mass and base asset.</done>
</task>

<task type="auto">
  <name>Task 2: RGB Camera Configuration</name>
  <files>arcproLab/arcpro_env_cfg.py</files>
  <action>
    - Update `tiled_camera` (TiledCameraCfg) in `ARCProSceneCfg`.
    - Set `data_types=["rgb"]`.
    - Configure `width=160`, `height=90`.
    - Set the `prim_path` to match the camera location in `F1Tenth_Modern.usd` or spawn a new one at the specified offset: [0.145, 0.0, 0.145] relative to the rear axle link.
  </action>
  <verify>Camera resolution is 160x90 and data_type is RGB-only.</verify>
  <done>Per-agent camera configured for high-speed RL ingestion.</done>
</task>

</tasks>

<success_criteria>
- Mass = 4.092kg.
- Camera resolution = 160x90 RGB.
</success_criteria>
```
