---
phase: 05-training-policy-development
plan: 05-02
type: execute
wave: 1
depends_on: [05-01]
files_modified: [
  "arcproLab/mdp/observations.py",
  "arcproLab/mdp/rewards.py",
  "arcproLab/mdp/events.py",
  "arcproLab/arcpro_env_cfg.py",
  "arcproLab/mdp/policy_wrapper.py"
]
autonomous: true
requirements: [REQ-SIM-METRIC, REQ-NAV-GRAPH, REQ-NAV-TRANSITION]
---

<objective>
Implement the full hierarchical policy stack and 12-float telemetry protocol in the 1.0x metric environment.
This involves updating the Markov Decision Process (MDP) components to match legacy ARCPro specifications and integrating the path planning/control modules from the sibling codebase.

Purpose: Enable high-fidelity autonomous navigation and route planning.
Output: Integrated hierarchical policy stack with verified telemetry and rewards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@arcproLab/mdp/observations.py
@arcproLab/mdp/rewards.py
@arcproLab/mdp/events.py
@../arc_rl_isacc_policy/policies/hierarchical_policy.py
@../arc_rl_isacc_policy/agent/agent_node.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 12-Float Telemetry & Hybrid Racer Rewards</name>
  <files>arcproLab/mdp/observations.py, arcproLab/mdp/rewards.py, arcproLab/arcpro_env_cfg.py</files>
  <action>
    - Update `arcproLab/mdp/observations.py` to implement the full 12-float protocol:
        - [0]: turn_token (from env.extras or default 0.0)
        - [1]: go_signal (from env.extras or default 1.0)
        - [2]: goal_dist (0.0)
        - [3]: forward_speed (m/s)
        - [4]: yaw_rate (rad/s)
        - [5]: last_steer
        - [6]: last_throttle
        - [7]: last_brake (0.0)
        - [8]: lateral_error (m)
        - [9]: heading_error (rad)
        - [10]: kappa (curvature from TrackManager)
        - [11]: cumulative_distance (m)
    - Implement `hybrid_racer_reward` in `arcproLab/mdp/rewards.py`:
        - `lane_reward = 2.0 * exp(-(lat_err^2) / 0.25)`
        - `speed_reward = speed * 2.0`
        - Total = `lane_reward + speed_reward - collision_penalty`
    - Update `arcproLab/arcpro_env_cfg.py` to use `hybrid_racer_reward` and ensure `ObservationCfg` captures the 12-float vector.
  </action>
  <verify>
    <automated>python3 -c "from arcproLab.mdp.observations import get_telemetry_vector; import torch; print('Telemetry function exists')"</automated>
  </verify>
  <done>Telemetry indices 0-11 are correctly populated and Hybrid Racer rewards are active.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Lane-Aligned Spawning</name>
  <files>arcproLab/mdp/events.py</files>
  <action>
    - Update `reset_robot_to_lane` in `arcproLab/mdp/events.py` to use `TrackManager`.
    - Logic:
        1. Select a random waypoint from `TrackManager.waypoints`.
        2. Apply a random lateral offset (Gaussian, sigma=0.15m).
        3. Apply a random heading offset (Gaussian, sigma=5.0 degrees).
        4. Teleport the robot to the resulting pose (grounded at Z=0.0).
        5. Zero out all velocities.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/verify_spawn.py</automated>
  </verify>
  <done>Robots spawn reliably on the track with randomized offsets, grounded at Z=0.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Hierarchical Policy Stack</name>
  <files>arcproLab/mdp/policy_wrapper.py, arcproLab/policy_stack/</files>
  <action>
    - Create `arcproLab/policy_stack/` directory.
    - Copy core modules from `../arc_rl_isacc_policy/` to `arcproLab/policy_stack/`:
        - `policies/hierarchical_policy.py`
        - `policies/fusion_policy.py`
        - `agent/agent_node.py`
        - `agent/intersection_graph.py`
    - Update `arcproLab/mdp/policy_wrapper.py` to load the `HierarchicalPathPlanningPolicy` and manage the `AgentNode` state machine.
    - Implement `HierarchicalPolicyWrapper` class that:
        - Loads SB3 model from `arc_rl_isacc_policy/models/isaac_hppo_20260223_210131/final_model.zip`.
        - Provides `predict_action(obs)` which runs the Worker -> Enriches Obs -> Runs Policy -> Gates Action.
  </action>
  <verify>
    <automated>python3 -c "from arcproLab.mdp.policy_wrapper import HierarchicalPolicyWrapper; print('Wrapper integrated')"</automated>
  </verify>
  <done>Hierarchical stack is integrated and accessible for inference.</done>
</task>

<task type="auto">
  <name>Task 4: Verify Full Stack at 1.0x Scale</name>
  <files>arcproLab/scripts/verify_policy.py</files>
  <action>
    - Update/Create `arcproLab/scripts/verify_policy.py` to use the `HierarchicalPolicyWrapper`.
    - Run simulation in GUI mode (if possible) or recording telemetry.
    - Verify:
        - Robot maintains lane at 1.0x metric scale.
        - Worker identifies intersections and picks turns.
        - Telemetry indices are stable (no NaNs).
  </action>
  <verify>
    <automated>./verify_sim_metric.sh</automated>
  </verify>
  <done>Full hierarchical stack verified performing autonomous navigation in the metric environment.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Observation vector has 12 floats matching the legacy protocol"
    - "Robot spawns on the track with randomized lateral/heading offsets"
    - "Rewards follow a Gaussian distribution centered on the lane (Hybrid Racer)"
    - "Worker successfully identifies and navigates intersections"
  artifacts:
    - path: "arcproLab/mdp/observations.py"
      provides: "12-float telemetry vector"
    - path: "arcproLab/mdp/rewards.py"
      provides: "Hybrid Racer rewards"
    - path: "arcproLab/policy_stack/hierarchical_policy.py"
      provides: "Hierarchical policy architecture"
    - path: "arcproLab/mdp/policy_wrapper.py"
      provides: "Inference interface for hierarchical stack"
  key_links:
    - from: "arcproLab/mdp/observations.py"
      to: "TrackManager"
      via: "compute_errors"
    - from: "arcproLab/mdp/policy_wrapper.py"
      to: "AgentNode"
      via: "worker_step and prepare_obs"
</must_haves>

<success_criteria>
- Telemetry indices 0-11 are correctly populated and verified.
- Spawning logic correctly resets robots to random track positions.
- Hierarchical policy stack performs inference and controls the robot.
- Verification script confirms successful track following and intersection navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-policy-development/05-02-SUMMARY.md`
</output>
