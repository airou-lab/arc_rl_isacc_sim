---
phase: 06-intersection-graph-navigation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [arcproLab/scripts/traffic_light_controller.py, openStreetUSD/arcpro_RL_open_street_sim.usd]
autonomous: false
requirements: [REQ-INT-LIGHT, REQ-INT-CTRL]

must_haves:
  truths:
    - "Traffic lights are present at road nodes"
    - "Traffic light colors change via ROS 2 commands"
    - "USD Variants (Red, Green) are correctly switched"
  artifacts:
    - path: "arcproLab/scripts/traffic_light_controller.py"
      provides: "ROS 2 traffic light controller"
    - path: "openStreetUSD/arcpro_RL_open_street_sim.usd"
      provides: "Traffic light assets in simulation"
  key_links:
    - from: "arcproLab/scripts/traffic_light_controller.py"
      to: "USD Stage"
      via: "omni.usd.set_variant_selection"
---

<objective>
Integrate functional traffic lights into the simulation environment and implement their ROS 2 control mechanism.

Purpose: Provide realistic intersection control for training and verification.
Output: Scene with traffic lights and a functional ROS 2 controller.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/RESEARCH_PHASE_06_INTERSECTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add traffic light assets to USD scene</name>
  <files>openStreetUSD/arcpro_RL_open_street_sim.usd</files>
  <action>
    Modify `openStreetUSD/arcpro_RL_open_street_sim.usd` (or create a script to do it dynamically) to include the "Rivermark" traffic light asset from the Isaac Sim content library.
    - Place traffic lights at the coordinates identified as nodes (intersections) in the RoadGraph.
    - Ensure each traffic light prim has the 'state' USD variant set with Red, Yellow, Green options.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/check_prim.py --prim /World/TrafficLight_01</automated>
  </verify>
  <done>Traffic light assets are correctly placed in the USD stage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ROS 2 TrafficLightController</name>
  <files>arcproLab/scripts/traffic_light_controller.py</files>
  <action>
    Implement a ROS 2 node using `rclpy` and the Isaac Sim Python API (`pxr`, `omni.usd`).
    - The node should subscribe to a topic (e.g., `/traffic_light_state`) with a message type of `std_msgs/String`.
    - Upon receiving a state ("red", "green"), it should find the corresponding USD prim and update its "state" variant selection.
    - Use the logic outlined in `docs/RESEARCH_PHASE_06_INTERSECTIONS.md`.
  </action>
  <verify>
    <automated>ros2 run arcproLab traffic_light_controller --ros-args -p prim_path:=/World/TrafficLight_01</automated>
  </verify>
  <done>Traffic light controller node is functional and correctly updates USD variants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Visual traffic light control</what-built>
  <how-to-verify>
    1. Start the simulation with Isaac Sim.
    2. Run the traffic_light_controller node.
    3. Publish a command: `ros2 topic pub /traffic_light_state std_msgs/String "data: 'red'" -1`
    4. Observe the traffic light in the viewport. It should turn RED.
    5. Publish "green" and verify it turns GREEN.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- Traffic lights appear in the Isaac Sim viewport.
- Traffic light color changes when publishing to the ROS 2 topic.
- Isaac Sim's variant system is used for the state change.
</success_criteria>

<output>
After completion, create `.planning/phases/06-intersection-graph-navigation/06-02-SUMMARY.md`
</output>
