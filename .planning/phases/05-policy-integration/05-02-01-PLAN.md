---
phase: 05-policy-integration
plan: 05-02-01
type: execute
wave: 1
depends_on: []
files_modified: [arcproLab/mdp/track_manager.py, arcproLab/mdp/observations.py]
autonomous: true
requirements: [TELEMETRY-01, ODOMETRY-01]

must_haves:
  truths:
    - "Index 2 (Goal Dist) is always 0.0"
    - "Index 10 (Kappa) correctly reflects track curvature"
    - "Index 11 (Odometry) accumulates and resets per episode"
  artifacts:
    - path: "arcproLab/mdp/track_manager.py"
      provides: "3-point circle-fit curvature calculation"
    - path: "arcproLab/mdp/observations.py"
      provides: "Full 12-float telemetry protocol implementation"
  key_links:
    - from: "arcproLab/mdp/observations.py"
      to: "arcproLab/mdp/track_manager.py"
      via: "compute_errors returning kappa"
---

<objective>
Implement the advanced 12-float telemetry protocol and high-fidelity curvature calculation.

Purpose: Provide the policy with essential environmental context and physics feedback.
Output: Updated TrackManager and ObservationManager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@arcproLab/mdp/track_manager.py
@arcproLab/mdp/observations.py

<interfaces>
From arcproLab/mdp/track_manager.py:
```python
def compute_errors(self, pos: torch.Tensor, yaw: torch.Tensor) -> Tuple[torch.Tensor, torch.Tensor]:
    # Returns (lat_err, head_err)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Curvature (Kappa) in TrackManager</name>
  <files>arcproLab/mdp/track_manager.py</files>
  <behavior>
    - For straight line (Area=0), Kappa should be 0.0.
    - For circle of radius R, Kappa should be 1/R.
    - Calculation uses 3 points: closest waypoint, and its two neighbors.
  </behavior>
  <action>
    - Update `compute_errors` to also return `kappa`.
    - Implement 3-point circle-fit:
      - Points: P1 (prev), P2 (current), P3 (next).
      - Area A = 0.5 * |x1(y2-y3) + x2(y3-y1) + x3(y1-y2)|
      - Lengths: a, b, c (P1-P2, P2-P3, P3-P1).
      - R = (abc) / (4A) if A > 1e-6 else inf.
      - Kappa = 1/R.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/verify_metric.py --check-curvature</automated>
  </verify>
  <done>TrackManager returns correct curvature for various track segments.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Full 12-Float Telemetry & Odometry</name>
  <files>arcproLab/mdp/observations.py</files>
  <action>
    - Update `get_telemetry_vector` to implement all indices:
      - Index 2: Hardcode to 0.0 (Endless Mode per D-01).
      - Index 10: Kappa from TrackManager (per D-02).
      - Index 11: Accumulated distance (Odometry per D-03).
    - Implement Odometry Accumulator:
      - Store `last_root_pos` in `env.extras`.
      - distance += norm(pos - last_root_pos) * (1 - reset_buf).
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/verify_policy.py --check-telemetry</automated>
  </verify>
  <done>Telemetry vector matches the 12-float protocol exactly.</done>
</task>

</tasks>

<verification>
Run `verify_policy.py` and ensure Index 2 is 0.0, Index 10 changes on curves, and Index 11 increases while moving.
</verification>

<success_criteria>
- Telemetry vector (1x12) is correctly populated.
- No NaNs in observation.
- Odometry resets on env.reset().
</success_criteria>

<output>
After completion, create `.planning/phases/05-policy-integration/05-02-01-SUMMARY.md`
</output>
