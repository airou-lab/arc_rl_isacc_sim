---
phase: 05-policy-integration
plan: 05-02-02
type: execute
wave: 1
depends_on: []
files_modified: [arcproLab/policy_stack/fusion_policy.py]
autonomous: true
requirements: [POLICY-01, POLICY-02]

must_haves:
  truths:
    - "Model uses ResNet18 architecture for vision"
    - "Visual embedding dimension is 256"
    - "Telemetry vector is fused (concatenated) with visual features"
    - "LayerNorm stabilizes the 268-dim fused output"
  artifacts:
    - path: "arcproLab/policy_stack/fusion_policy.py"
      provides: "ResNet18-based multi-modal fusion network"
  key_links:
    - from: "arcproLab/policy_stack/fusion_policy.py"
      to: "torchvision.models.resnet18"
      via: "model instantiation"
---

<objective>
Replace the legacy NatureCNN visual stream with a ResNet18-based features extractor.

Purpose: Leverage modern residual learning for more robust lane-feature extraction.
Output: Refactored FusionFeaturesExtractor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@arcproLab/policy_stack/fusion_policy.py

<interfaces>
From arcproLab/policy_stack/fusion_policy.py:
```python
class FusionFeaturesExtractor(BaseFeaturesExtractor):
    def __init__(self, observation_space: spaces.Dict, features_dim: int = 268):
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace NatureCNN with ResNet18</name>
  <files>arcproLab/policy_stack/fusion_policy.py</files>
  <action>
    - Import `torchvision.models.resnet18` (use `.venv/bin/python3` as interpreter context).
    - Refactor `FusionFeaturesExtractor.__init__`:
      - Initialize ResNet18.
      - If pretrained weights desired (per D-04), use `weights=ResNet18_Weights.DEFAULT`.
      - Modify/Replace the final `fc` layer to output 256 features.
      - Ensure input layer handles 3-channel (RGB) images of 90x160.
  </action>
  <verify>
    <automated>./.venv/bin/python3 -c "import torch; from arcproLab.policy_stack.fusion_policy import FusionFeaturesExtractor; from gymnasium import spaces; space = spaces.Dict({'image': spaces.Box(0, 255, (90, 160, 3), 'uint8'), 'vec': spaces.Box(-1, 1, (12,), 'float32')}); model = FusionFeaturesExtractor(space); print(model.features_dim)"</automated>
  </verify>
  <done>FusionFeaturesExtractor successfully instantiates ResNet18 and outputs correct dimensions.</done>
</task>

<task type="auto">
  <name>Task 2: Update Fusion and LayerNorm</name>
  <files>arcproLab/policy_stack/fusion_policy.py</files>
  <action>
    - Update `forward` method:
      - Process image through ResNet18 to get 256-dim embedding.
      - Concatenate with 12-dim telemetry.
      - Apply `self.fusion_norm` (ensure dimension is 268).
  </action>
  <verify>
    <automated>./.venv/bin/python3 -c "import torch; from arcproLab.policy_stack.fusion_policy import FusionFeaturesExtractor; from gymnasium import spaces; space = spaces.Dict({'image': spaces.Box(0, 255, (90, 160, 3), 'uint8'), 'vec': spaces.Box(-1, 1, (12,), 'float32')}); model = FusionFeaturesExtractor(space); img = torch.zeros(1, 3, 90, 160); vec = torch.zeros(1, 12); out = model.forward({'image': img, 'vec': vec}); print(out.shape)"</automated>
  </verify>
  <done>Forward pass produces (1, 268) tensor with no NaNs.</done>
</task>

</tasks>

<verification>
Instantiate the extractor in a script and pass dummy tensors to verify no dimension errors.
</verification>

<success_criteria>
- ResNet18 backbone used.
- Output features_dim = 268.
- LayerNorm applied correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-policy-integration/05-02-02-SUMMARY.md`
</output>
