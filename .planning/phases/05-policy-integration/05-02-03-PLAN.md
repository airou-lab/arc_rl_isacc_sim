---
phase: 05-policy-integration
plan: 05-02-03
type: execute
wave: 2
depends_on: [05-02-01]
files_modified: [arcproLab/mdp/events.py]
autonomous: true
requirements: [SPAWN-01]

must_haves:
  truths:
    - "Robot resets to a random lane waypoint"
    - "Spawn position has +/- 10cm lateral jitter"
    - "Spawn heading has +/- 5-degree noise"
  artifacts:
    - path: "arcproLab/mdp/events.py"
      provides: "High-fidelity randomized lane-aligned reset logic"
  key_links:
    - from: "arcproLab/mdp/events.py"
      to: "arcproLab/mdp/track_manager.py"
      via: "waypoint lookup for reset"
---

<objective>
Implement randomized, lane-aligned spawning with lateral and heading noise to improve policy robustness.

Purpose: Prevent overfitting to a single start point and simulate realistic deployment variation.
Output: Vectorized reset logic in events.py.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@arcproLab/mdp/events.py
@arcproLab/mdp/track_manager.py

<interfaces>
From arcproLab/mdp/track_manager.py:
```python
def get_closest_waypoint_data(self, pos: torch.Tensor) -> torch.Tensor:
    # returns [x, y, yaw]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lane-Aligned Teleportation Logic</name>
  <files>arcproLab/mdp/events.py</files>
  <action>
    - Refactor `reset_robot_to_lane`.
    - Select random waypoint index for each env from `TrackManager.waypoints`.
    - Extract [x, y, yaw] from selected waypoints.
    - Set Z = 0.0514 (True Physics wheel radius).
    - Convert yaw to quaternion [qw, qx, qy, qz].
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/verify_spawn.py --count 10</automated>
  </verify>
  <done>Robot successfully teleports to random track locations on reset.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Lateral Jitter and Heading Noise</name>
  <files>arcproLab/mdp/events.py</files>
  <action>
    - Apply lateral jitter (D-05):
      - jitter = (rand() - 0.5) * 0.2  # +/- 10cm
      - pos += jitter * normal_vector(yaw)
    - Apply heading noise (D-05):
      - noise = (rand() - 0.5) * 10 * (pi/180) # +/- 5 degrees
      - yaw += noise
    - Ensure `env.extras["distance"]` is reset for the given `env_ids`.
    - Write root pose and zero root velocity to sim.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/verify_spawn.py --check-noise</automated>
  </verify>
  <done>Spawns exhibit correct lateral variance (+/- 10cm) and angular variance (+/- 5 deg).</done>
</task>

</tasks>

<verification>
Run `verify_spawn.py` multiple times and observe robot positions relative to track centerline.
</verification>

<success_criteria>
- Robot spawns on the track.
- Observable noise in position and heading.
- Odometry (Index 11) is 0.0 immediately after reset.
</success_criteria>

<output>
After completion, create `.planning/phases/05-policy-integration/05-02-03-SUMMARY.md`
</output>
