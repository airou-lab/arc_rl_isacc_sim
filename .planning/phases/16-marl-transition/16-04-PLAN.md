---
phase: 16-marl-transition
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified: [arcproLab/scripts/stress_test_vram.py, arcproLab/scripts/verify_marl.py]
autonomous: true
requirements: [MARL-05]
must_haves:
  truths:
    - "VRAM usage is stable < 8GB during multi-agent training."
    - "Physics at 500Hz (dt=0.002) is stable during agent collisions."
    - "FIFO Go-Signal correctly alternates precedence between 2 agents."
  artifacts:
    - path: "arcproLab/scripts/verify_marl.py"
      provides: "Functional coordination verification"
---

<objective>
Perform exhaustive validation of the MARL stack, focusing on VRAM stability, physics integrity during interaction, and the correctness of the FIFO coordination logic.

Purpose: Final production sign-off for MARL.
Output: Validation report and verification scripts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: VRAM and Physics Stress Test</name>
  <files>arcproLab/scripts/stress_test_vram.py</files>
  <action>
    - Create a script that initializes 8 envs x 2 agents and performs 1000 random steps.
    - Monitor VRAM usage (using `nvidia-smi` or `torch.cuda.memory_allocated`).
    - Audit physics stability: Check for "exploding" velocities or NaNs in root_pos_w during collisions at 500Hz.
  </action>
  <verify>
    <automated># Check log for NaNs or OOM</automated>
  </verify>
  <done>System is verified stable at 16 agents / 500Hz physics.</done>
</task>

<task type="auto">
  <name>Task 2: FIFO Coordination Functional Verification</name>
  <files>arcproLab/scripts/verify_marl.py</files>
  <action>
    - Create a verification script that forces two agents onto a collision course at an intersection.
    - Verify that the first agent to reach the trigger zone receives `Go=1` and the second receives `Go=0`.
    - Verify that the second agent's signal flips to `Go=1` only after the first agent clears the exit gate.
    - Verify that a reset of Agent A releases the token to Agent B.
  </action>
  <verify>
    <automated># Assert go_signal changes in log</automated>
  </verify>
  <done>FIFO Go-Signal logic is functionally correct and reset-safe.</done>
</task>

</tasks>

<success_criteria>
- Zero OOM crashes during 1000 steps of multi-agent execution.
- No physics instabilities detected at 500Hz.
- FIFO logic prevents intersection deadlocks and correctly handles agent resets.
</success_criteria>

<output>
After completion, create `.planning/phases/16-marl-transition/16-04-SUMMARY.md`
</output>
