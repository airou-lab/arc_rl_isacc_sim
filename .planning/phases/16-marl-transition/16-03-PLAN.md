---
phase: 16-marl-transition
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified: [arcproLab/arcpro_multi_env_cfg.py, arcproLab/policy_stack/skrl_marl_model.py, arcproLab/policy_stack/train_marl.py]
autonomous: true
requirements: [MARL-03, MARL-04]
must_haves:
  truths:
    - "Environment spawns 2 robots per environment clone (8 envs x 2 agents = 16 cameras)."
    - "TiledCamera captures RGB views for all 16 agents in a single texture."
    - "SKRL Model correctly reshapes 5D tensors (B, N, C, H, W) for the Adaptive CNN."
  artifacts:
    - path: "arcproLab/arcpro_multi_env_cfg.py"
      provides: "8x2 Multi-agent environment configuration"
    - path: "arcproLab/policy_stack/skrl_marl_model.py"
      provides: "Native SKRL Multi-Agent Model with shared CNN backbone"
---

<objective>
Configure the multi-agent scene setup and implement the SKRL-native policy architecture. This involves transitioning from SB3 to SKRL to support 5D vision tensors and native multi-agent GPU rollouts.

Purpose: Launch MARL training with SKRL.
Output: SKRL-native MARL config and training script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/17-RESEARCH_MARL_STACK.md
@arcproLab/arcpro_env_cfg.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8x2 Multi-Agent configuration</name>
  <files>arcproLab/arcpro_multi_env_cfg.py</files>
  <action>
    - Create `arcproLab/arcpro_multi_env_cfg.py`.
    - Set `num_envs=8` and configure 2 robots per environment using regex `Robot_[0-1]`.
    - Total cameras = 16 (Target VRAM 6.2GB).
    - Ensure `TiledCamera` horizontal/vertical apertures are correctly sized for 16 views.
  </action>
  <verify>
    <automated>python -c "from arcproLab.arcpro_multi_env_cfg import ARCProMultiEnvCfg; cfg = ARCProMultiEnvCfg(); print('8x2 MARL config created')"</automated>
  </verify>
  <done>8x2 MARL environment configuration exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SKRL-native Multi-Agent Model</name>
  <files>arcproLab/policy_stack/skrl_marl_model.py</files>
  <action>
    - Implement `ARCProMARLModel` inheriting from `skrl.models.torch.GaussianMixin` and `skrl.models.torch.Model`.
    - Input Shape: Dict with `image: (B, N, C, H, W)` and `vec: (B, N, 12+6)`.
    - Implement `compute(self, inputs, role)` to reshape image to 4D, pass through CNN, and cat with V2V-augmented telemetry.
    - CRITICAL: Ensure `AdaptiveAvgPool2d((128, 128))` is the first layer.
  </action>
  <verify>
    <automated># Manual verification via model unit test</automated>
  </verify>
  <done>SKRL Multi-Agent Model handles 5D tensors with early pooling.</done>
</task>

<task type="auto">
  <name>Task 3: Create SKRL MARL Training Script</name>
  <files>arcproLab/policy_stack/train_marl.py</files>
  <action>
    - Create `train_marl.py` using SKRL's `MAPPO` or `IPPO` trainer.
    - Configure the `IsaacLabWrapper` (or SKRL native equivalent) to handle the multi-agent environment.
    - Set up the rollout buffer to use `uint8` for images if SKRL permits, or ensure early pooling mitigates the float32 footprint.
    - Add a "Pre-trained Weights" loader to bootstrap the CNN from Phase 15.
  </action>
  <verify>
    <automated># Dry run to check VRAM usage</automated>
  </verify>
  <done>SKRL training script is ready for Milestone 4.</done>
</task>

</tasks>

<success_criteria>
- Isaac Lab spawns 16 agents total (8 envs x 2 agents).
- SKRL Model processes 5D vision/telemetry batches without OOM.
- Training loop initializes on the GPU without CPU-GPU bottlenecks.
</success_criteria>

<output>
After completion, create `.planning/phases/16-marl-transition/16-03-SUMMARY.md`
</output>
