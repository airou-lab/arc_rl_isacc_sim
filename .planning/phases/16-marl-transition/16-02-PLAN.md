---
phase: 16-marl-transition
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [arcproLab/mdp/rewards.py, arcproLab/mdp/terminations.py]
autonomous: true
requirements: [MARL-01, MARL-02]
must_haves:
  truths:
    - "Rewards are computed correctly for a batch of (num_envs * num_agents) robots."
    - "Terminations (resets) are triggered correctly for individual agents or environments."
    - "Robot-to-robot collisions trigger environment resets."
  artifacts:
    - path: "arcproLab/mdp/rewards.py"
      provides: "Vectorized reward functions for multi-agent batches"
    - path: "arcproLab/mdp/terminations.py"
      provides: "Vectorized termination functions including collision detection"
  key_links:
    - from: "arcproLab/mdp/rewards.py"
      to: "arcproLab/mdp/track_manager.py"
      via: "lateral_error_reward"
    - from: "arcproLab/mdp/terminations.py"
      to: "isaaclab.sensors.ContactSensor"
      via: "robot-to-robot collision detection"
---

<objective>
Audit and update the MDP Reward and Termination functions to handle vectorized multi-agent batches. Implement robot-to-robot collision detection to ensure agents learn to coordinate and avoid each other.

Purpose: Correct reward/termination logic for MARL scaling.
Output: Updated mdp/rewards.py and mdp/terminations.py.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/16-RESEARCH.md
@arcproLab/mdp/rewards.py
@arcproLab/mdp/terminations.py
@arcproLab/mdp/track_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vectorize reward functions</name>
  <files>arcproLab/mdp/rewards.py</files>
  <action>
    - Update all reward functions (`speed_reward`, `lateral_error_reward`, etc.) to handle the total batch size $B = num\_envs \times num\_agents$.
    - Replace `env.num_envs` with the actual batch size derived from the asset data or `env.num_instances`.
    - Ensure `env.extras["lat_err"]` and other buffered values have the correct shape.
    - Specifically, update `action_rate_smoothness_reward` to handle multi-agent actions.
  </action>
  <verify>
    <automated># Manual verification via dry-run of reward computation</automated>
  </verify>
  <done>Reward functions support multi-agent batch sizes without shape mismatches.</done>
</task>

<task type="auto">
  <name>Task 2: Vectorize termination functions</name>
  <files>arcproLab/mdp/terminations.py</files>
  <action>
    - Update `white_line_contact`, `height_termination`, and others to support multi-agent batches.
    - Ensure `dist_g` (gate distance) and `alignment_ok` logic uses vectorized tensors for the whole batch.
    - Fix any logic that assumes one robot per environment (e.g., world pos to local pos conversion).
  </action>
  <verify>
    <automated># Manual verification via dry-run of termination logic</automated>
  </verify>
  <done>Termination functions support multi-agent batch sizes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement robot-to-robot collision detection</name>
  <files>arcproLab/mdp/terminations.py</files>
  <action>
    - Add a new termination function `robot_collision` in `mdp/terminations.py`.
    - Use `env.scene["contact_forces"].data.net_forces_w` to detect collisions between robot prims.
    - Since robots are in the same environment, filter contact forces to identify when a robot hits another robot (not just the road or boundaries).
    - Trigger termination for the entire environment if a collision between any two robots occurs (strict coordination training).
  </action>
  <verify>
    <automated># Manual verification: Spawn two robots close to each other and check if termination triggers</automated>
  </verify>
  <done>Multi-agent collision detection is implemented and triggers resets.</done>
</task>

</tasks>

<success_criteria>
- All reward and termination functions are fully vectorized for multi-agent batches.
- Robot-to-robot collisions are detected and trigger environment resets.
</success_criteria>

<output>
After completion, create `.planning/phases/16-marl-transition/16-02-SUMMARY.md`
</output>
