---
phase: 16-marl-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [arcproLab/mdp/road_manager.py, arcproLab/mdp/track_manager.py, arcproLab/mdp/observations.py]
autonomous: true
requirements: [MARL-01, MARL-02]
must_haves:
  truths:
    - "RoadManager supports independent turn tokens for (num_envs, num_agents)."
    - "RoadManager implements Reset-Aware Token Clearing for the FIFO Go-Signal queue."
    - "TrackManager computes lateral errors for all agents in a single vectorized pass."
  artifacts:
    - path: "arcproLab/mdp/road_manager.py"
      provides: "Vectorized navigation intent and FIFO coordination"
    - path: "arcproLab/mdp/track_manager.py"
      provides: "Vectorized error calculation with tensor offsets"
---

<objective>
Refactor the singleton-based navigation and track management systems into vectorized, instance-based managers. This transition allows the simulation to track independent navigation intents, FIFO intersection queues, and tracking errors for multiple agents simultaneously.

Purpose: Enable independent agent states and coordination.
Output: RoadManager and updated TrackManager supporting (B, N) tensor shapes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/17-RESEARCH_MARL_STACK.md
@arcproLab/mdp/road_graph.py
@arcproLab/mdp/track_manager.py
@arcproLab/mdp/observations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoadManager with FIFO and Reset-Aware clearing</name>
  <files>arcproLab/mdp/road_manager.py</files>
  <action>
    - Create `RoadManager` class in `arcproLab/mdp/road_manager.py`.
    - Implement `turn_token` and `go_signal` as tensors of shape `(num_envs, num_agents)`.
    - Implement a FIFO queue logic for intersection approach zones.
    - CRITICAL: Add a `handle_resets(self, reset_buf)` method that clears any active "Go" tokens or queue positions for agents that have reset.
    - Update `get_nav_commands()` to return agent-specific signals.
  </action>
  <verify>
    <automated>python -c "from arcproLab.mdp.road_manager import RoadManager; rm = RoadManager(); print('RoadManager initialized')"</automated>
  </verify>
  <done>RoadManager class exists and supports multi-agent FIFO coordination with reset safety.</done>
</task>

<task type="auto">
  <name>Task 2: Vectorize TrackManager for multi-agent offsets</name>
  <files>arcproLab/mdp/track_manager.py</files>
  <action>
    - Refactor `TrackManager` to remove the global `_TRACK_MANAGER` singleton.
    - Update `compute_errors` to accept `target_lane_offset` as a `torch.Tensor` of shape `(B,)` where $B = num\_envs \times num\_agents$.
    - Use vectorized PyTorch operations to apply offsets to the lateral error for the entire batch.
  </action>
  <verify>
    <automated>python -c "import torch; from arcproLab.mdp.track_manager import TrackManager; tm = TrackManager(); pos=torch.zeros((2,3)); yaw=torch.zeros(2); off=torch.tensor([0.1, 0.2]); tm.compute_errors(pos, yaw, off)"</automated>
  </verify>
  <done>TrackManager supports vectorized error calculation with per-agent offsets.</done>
</task>

<task type="auto">
  <name>Task 3: Implement V2V Relative Coordination Observation</name>
  <files>arcproLab/mdp/observations.py</files>
  <action>
    - Implement `get_v2v_observations(env)` in `mdp/observations.py`.
    - Logic: For each agent, find its peer in the same environment.
    - Calculate relative `[dx, dy, dyaw, dvx, dvy]` in the **ego-agent's local frame** using quaternion rotation utilities.
    - Append the `go_signal` status from `RoadManager`.
    - Return a 6-dim tensor per agent.
  </action>
  <verify>
    <automated># Manual verification via training script dry-run or unit test for observations</automated>
  </verify>
  <done>Observations correctly calculate relative peer state in ego-local frames.</done>
</task>

</tasks>

<success_criteria>
- RoadManager and TrackManager are successfully refactored into instance-based classes.
- FIFO Go-Signal precedence is active with reset-aware token clearing.
- V2V observations provide relative peer state in the ego-local frame.
</success_criteria>

<output>
After completion, create `.planning/phases/16-marl-transition/16-01-SUMMARY.md`
</output>
