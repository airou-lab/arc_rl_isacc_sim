---
phase: 10-intersection-navigation
plan: 01
type: execute
wave: 1
depends_on: [09]
files_modified: [arcproLab/mdp/road_graph.py, arcproLab/mdp/track_manager.py]
autonomous: true
requirements: [REQ-NAV-GRAPH]

must_haves:
  truths:
    - "Road segments are correctly identified from USD"
    - "Graph nodes represent road intersections"
    - "Each robot is associated with a specific road edge"
  artifacts:
    - path: "arcproLab/mdp/road_graph.py"
      provides: "Graph structure for road segments"
    - path: "arcproLab/mdp/track_manager.py"
      provides: "Multi-segment track management"
  key_links:
    - from: "arcproLab/mdp/track_manager.py"
      to: "arcproLab/mdp/road_graph.py"
      via: "edge traversal"
---

<objective>
Implement a graph-based representation of the road network to support multi-segment navigation and complex intersections.

Purpose: Enable robots to move beyond single tracks and navigate complex layouts.
Output: RoadGraph class and enhanced TrackManager.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@arcproLab/mdp/track_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement RoadGraph data structure</name>
  <files>arcproLab/mdp/road_graph.py</files>
  <action>
    Create a RoadGraph class that holds nodes (coordinates) and edges (segment definitions).
    - Each edge should reference a list of waypoints (x, y, yaw).
    - Provide methods for finding the next edge at a node.
    - Support loading/saving to a simple JSON format.
  </action>
  <verify>
    <automated>python3 -c "from arcproLab.mdp.road_graph import RoadGraph; g = RoadGraph(); g.add_node(0, (0,0)); print('Success')"</automated>
  </verify>
  <done>RoadGraph class implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance TrackManager for multi-segment support</name>
  <files>arcproLab/mdp/track_manager.py</files>
  <action>
    Refactor TrackManager to:
    - Hold multiple segments instead of a single list of waypoints.
    - Support a "current_edge" per environment (num_envs).
    - Update `compute_errors` to use the current edge's waypoints.
    - Implement transition logic to switch edges when reaching the end of the current segment.
  </action>
  <verify>
    <automated>pytest tests/test_track_manager.py</automated>
  </verify>
  <done>TrackManager updated with multi-segment and transition logic.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-discover graph from USD</name>
  <files>arcproLab/mdp/road_graph.py, arcproLab/mdp/track_manager.py</files>
  <action>
    Implement `sample_graph_from_usd` which scans the USD stage for road meshes (identified by naming conventions like `road_seg_01`) and builds the RoadGraph edges automatically.
    - Connect edges based on coordinate proximity of endpoints.
    - Update TrackManager initialization to use this auto-discovery.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/audit_track_collisions.py</automated>
  </verify>
  <done>Auto-discovery of road graph from USD stage is functional.</done>
</task>

</tasks>

<success_criteria>
- RoadGraph can store and retrieve multi-segment paths.
- TrackManager correctly computes lateral/heading errors for robots on different segments.
- Segments are automatically identified from the Isaac Sim scene.
</success_criteria>

<output>
After completion, create `.planning/phases/06-intersection-graph-navigation/06-01-SUMMARY.md`
</output>
