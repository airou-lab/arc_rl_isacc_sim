---
phase: 10-intersection-navigation
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified: [arcproLab/mdp/observations.py, arcproLab/mdp/rewards.py, arcproLab/arcpro_env_cfg.py]
autonomous: true
requirements: [REQ-NAV-TRANSITION, REQ-INT-OBS, REQ-INT-REWARD]

must_haves:
  truths:
    - "RL agent receives traffic light state in observations"
    - "Reward function penalizes red light violations"
    - "Robots switch segments smoothly at intersections"
  artifacts:
    - path: "arcproLab/mdp/observations.py"
      provides: "Traffic light state in telemetry vector"
    - path: "arcproLab/mdp/rewards.py"
      provides: "Compliance-based rewards"
  key_links:
    - from: "arcproLab/arcpro_env_cfg.py"
      to: "arcproLab/mdp/observations.py"
      via: "ObservationGroupCfg registration"
---

<objective>
Update the RL environment with intersection-specific observations and rewards to enable autonomous navigation through complex networks.

Purpose: Teach the agent to respect traffic rules and navigate graph topology.
Output: Trained (or trainable) policy with intersection awareness.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@arcproLab/arcpro_env_cfg.py
@arcproLab/mdp/observations.py
@arcproLab/mdp/rewards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add traffic light state to observations</name>
  <files>arcproLab/mdp/observations.py, arcproLab/arcpro_env_cfg.py</files>
  <action>
    Update `mdp/observations.py` to include a new observation term: `traffic_light_state`.
    - This term should retrieve the current state (Red=0, Green=1) of the traffic light at the next node on the robot's current edge.
    - Register this term in `ARCProEnvCfg`'s `ObservationCfg`.
    - Append it to the `telemetry` vector or create a new observation group.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/verify_policy.py</automated>
  </verify>
  <done>Traffic light states are correctly integrated into the observation space.</done>
</task>

<task type="auto">
  <name>Task 2: Implement compliance rewards and penalties</name>
  <files>arcproLab/mdp/rewards.py, arcproLab/arcpro_env_cfg.py</files>
  <action>
    Update `mdp/rewards.py` with:
    - `red_light_penalty`: A large negative reward if the robot is moving when the relevant traffic light is Red.
    - `intersection_success_reward`: A positive reward for successfully transitioning between edges at an intersection.
    - Update `RewardCfg` in `arcpro_env_cfg.py` to include these terms.
  </action>
  <verify>
    <automated>python3 arcproLab/scripts/train_policy.py --steps 1000</automated>
  </verify>
  <done>Rewards for intersection compliance are implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification of graph navigation</name>
  <files>arcproLab/scripts/test_straight.py</files>
  <action>
    Create or update a test script to verify that a robot can:
    1. Traverse Edge A.
    2. Reach Node 1.
    3. Transition to Edge B (chosen randomly or via policy).
    4. Stop at Node 1 if the light is Red.
    5. Proceed when the light is Green.
  </action>
  <verify>
    <automated>pytest tests/test_track_manager.py</automated>
  </verify>
  <done>End-to-end graph navigation with traffic light compliance verified.</done>
</task>

</tasks>

<success_criteria>
- Robot's observation vector includes traffic light state.
- Robot stops or slows down at red lights during training.
- Robot successfully navigates from one edge to another through an intersection node.
</success_criteria>

<output>
After completion, create `.planning/phases/10-intersection-navigation/10-03-SUMMARY.md`
</output>
