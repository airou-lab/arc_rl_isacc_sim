---
phase: 09-training-stabilization
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [arcproLab/**/*.py]
autonomous: true
---

<objective>
Modernize the environment stack by migrating fully to Gymnasium and removing legacy 'gym' warnings.

Purpose: Clean up terminal output and ensure future compatibility.
Output: Project free of legacy 'gym' import warnings.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit and Replace Legacy Imports</name>
  <action>
    Search for `import gym` or `from gym import ...` and replace with `import gymnasium as gym`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Update Isaac Lab Env Calls</name>
  <action>
    Ensure `ManagerBasedRLEnv` and wrappers are called using Gymnasium-compliant patterns (e.g. observation/info dict unpacking).
  </action>
</task>

</tasks>
