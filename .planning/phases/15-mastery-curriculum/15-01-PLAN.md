---
phase: 15-mastery-curriculum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [arcproLab/mdp/terminations.py, arcproLab/arcpro_env_cfg.py]
autonomous: true
requirements: [REQ-MASTERY-BOUND]

must_haves:
  truths:
    - "Boundary margin is configurable via env_cfg or env.extras"
    - "Reward weights are updated to precision-first (LatErr: 20.0, Speed: 15.0)"
  artifacts:
    - path: "arcproLab/mdp/terminations.py"
      provides: "Parameterized boundary termination"
    - path: "arcproLab/arcpro_env_cfg.py"
      provides: "Mastery reward configuration"
  key_links:
    - from: "arcproLab/arcpro_env_cfg.py"
      to: "arcproLab/mdp/terminations.py"
      via: "roadmark_contact parameters"
---

<objective>
Refactor the environment termination logic and reward structure to support a precision-first mastery curriculum. This plan establishes the infrastructure for dynamic boundary tightening and reverts to stable, high-precision reward weights.

Purpose: Enable the agent to learn with wider margins initially while being heavily penalized for lateral deviations.
Output: Parameterized termination logic and updated RewardCfg.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@arcproLab/mdp/terminations.py
@arcproLab/arcpro_env_cfg.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize Boundary Termination</name>
  <files>arcproLab/mdp/terminations.py, arcproLab/arcpro_env_cfg.py</files>
  <action>
    - Modify `white_line_contact` in `terminations.py` to accept a `threshold: float = 0.13` parameter (defaulting to the current value).
    - Use this `threshold` instead of the hardcoded `0.13` for `dist_w` and `dist_y` checks.
    - Update `ARCProEnvCfg.terminations.roadmark_contact` in `arcpro_env_cfg.py` to pass `params={"threshold": 0.25}`.
    - Note: This allows the curriculum callback (Plan 02) to dynamically tighten this value by modifying `env.cfg.terminations.roadmark_contact.params["threshold"]` or reading from `env.extras`.
  </action>
  <verify>
    <automated>python3 -c "from arcproLab.mdp.terminations import white_line_contact; import inspect; print('threshold' in inspect.signature(white_line_contact).parameters)"</automated>
  </verify>
  <done>Termination function is parameterized and env_cfg is updated with the starting curriculum threshold (0.25).</done>
</task>

<task type="auto">
  <name>Task 2: Mastery Reward Tuning</name>
  <files>arcproLab/arcpro_env_cfg.py</files>
  <action>
    - Update `RewardCfg` in `arcpro_env_cfg.py` per user request:
      - `lateral_error` weight: 20.0 (up from 10.0)
      - `speed` weight: 15.0 (down from 25.0)
    - Keep `smoothness` at 30.0 and `stationary` at 2.0 to prevent jitter and crawling.
  </action>
  <verify>
    <automated>grep -E "lateral_error.*weight=20.0" arcproLab/arcpro_env_cfg.py && grep -E "speed.*weight=15.0" arcproLab/arcpro_env_cfg.py</automated>
  </verify>
  <done>Reward weights are updated to prioritize precision (20.0) over raw speed (15.0).</done>
</task>

</tasks>

<verification>
Check that `python3 arcproLab/verify.py` still runs (basic env sanity).
</verification>

<success_criteria>
- `white_line_contact` accepts and uses a threshold parameter.
- `RewardCfg` reflects the new mastery weights.
</success_criteria>

<output>
After completion, create `.planning/phases/15-mastery-curriculum/15-01-SUMMARY.md`
</output>
