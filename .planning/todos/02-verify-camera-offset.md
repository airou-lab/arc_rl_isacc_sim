# Todo: Verify Camera Offset
Goal: Ensure the 8.0x robot's camera is correctly positioned.

## Tasks
- [ ] Run `verify_spawn.py` with GUI and cameras enabled.
- [ ] Inspect the `tiled_camera` view in the viewport.
- [ ] Adjust `tiled_camera` offset in `arcpro_env_cfg.py` if the view is blocked by the chassis.
