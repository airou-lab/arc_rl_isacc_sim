---
phase: 07-true-physics-revert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - arcproLab/arcpro_env_cfg.py
  - arcproLab/arcpro_robot_cfg.py
  - arcproLab/mdp/events.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The F1Tenth robot appears at a realistic scale (1.0x metric)."
    - "The OpenStreetMap track appears proportionally scaled to realistic lane widths (~3.5m)."
    - "The robot spawns reliably on the track without initial collisions or excessive bouncing."
    - "The simulation physics behave realistically without the need for stability workarounds."
    - "The camera view is correctly positioned relative to the robot."
  artifacts:
    - path: "arcproLab/arcpro_env_cfg.py"
      provides: "Environment configuration for true physics"
      contains:
        - "asset.usd_path = "openStreetUSD/no_graph_sim_cleaned.usd""
        - "asset.scale = 0.0825"
        - "init_state.pos = [0.0, 0.0, 0.5]"
        - "physics.solver_position_iteration_count = 4"
        - "physics.solver_velocity_iteration_count = 1"
        - "camera.tiled_camera.offset = [0.28, 0.0, 0.16]"
    - path: "arcproLab/arcpro_robot_cfg.py"
      provides: "Robot asset configuration for true physics"
      contains:
        - "F1Tenth_Metric.usd"
    - path: "arcproLab/mdp/events.py"
      provides: "Robot stability event removal"
      excludes:
        - "setup_robot_stability"
  key_links:
    - from: "arcproLab/arcpro_env_cfg.py"
      to: "openStreetUSD/no_graph_sim_cleaned.usd"
      via: "asset.usd_path and asset.scale"
      pattern: "asset\.usd_path.*no_graph_sim_cleaned\.usd"
    - from: "arcproLab/arcpro_robot_cfg.py"
      to: "arcproLab/assets/robot/F1Tenth_Metric.usd"
      via: "robot_usd_path"
      pattern: "robot_usd_path.*F1Tenth_Metric\.usd"
    - from: "Simulation environment"
      to: "arcproLab/arcpro_env_cfg.py"
      via: "loading physics settings"
      pattern: "physics\.solver_position_iteration_count"
    - from: "Simulation environment"
      to: "arcproLab/arcpro_env_cfg.py"
      via: "loading camera offset"
      pattern: "camera\.tiled_camera\.offset"
    - from: "Simulation environment"
      to: "arcproLab/mdp/events.py"
      via: "event registration"
      pattern: "setup_robot_stability"
---

<objective>
Configure the simulation environment to use true-to-scale physics, a 1.0x metric F1Tenth robot, and a proportionally scaled OpenStreetMap track by modifying core configuration files and removing deprecated stability events.

Purpose: This phase reverts the simulation from previous workarounds (Giant Scale, high physics solver iterations) to a more realistic and transferable metric-scale environment, improving simulation fidelity.
Output: Updated configuration files (`arcproLab/arcpro_env_cfg.py`, `arcproLab/arcpro_robot_cfg.py`) and a modified event file (`arcproLab/mdp/events.py`) resulting in a metric-scale simulation.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-true-physics-revert/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Environment Physics, Scaling, and Camera Offset</name>
  <files>arcproLab/arcpro_env_cfg.py</files>
  <action>
    Modify `arcproLab/arcpro_env_cfg.py` to implement the following decisions:
    - **D-01 (Track Scaling Factor):** Set `asset.scale` for `openStreetUSD/no_graph_sim_cleaned.usd` to `0.0825`.
    - **D-02 (Robot Initial Spawn Height):** Set `init_state.pos` Z-coordinate to `0.5` (e.g., `init_state.pos = [0.0, 0.0, 0.5]`).
    - **D-03 (Physics Solver Settings):** Set `physics.solver_position_iteration_count = 4` and `physics.solver_velocity_iteration_count = 1`. Ensure `physics.solver_type` remains `TGS`.
    - **D-07 (Camera Offset):** Set `camera.tiled_camera.offset = [0.28, 0.0, 0.16]`.
  </action>
  <verify>
    <automated>
      grep -q "asset.scale = 0.0825" arcproLab/arcpro_env_cfg.py && 
      grep -q "init_state.pos = \[0.0, 0.0, 0.5\]" arcproLab/arcpro_env_cfg.py && 
      grep -q "physics.solver_position_iteration_count = 4" arcproLab/arcpro_env_cfg.py && 
      grep -q "physics.solver_velocity_iteration_count = 1" arcproLab/arcpro_env_cfg.py && 
      grep -q "camera.tiled_camera.offset = \[0.28, 0.0, 0.16\]" arcproLab/arcpro_env_cfg.py
    </automated>
  </verify>
  <done>
    `arcproLab/arcpro_env_cfg.py` is updated with the specified track scaling, robot spawn height, physics solver settings, and camera offset.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Robot Asset</name>
  <files>arcproLab/arcpro_robot_cfg.py</files>
  <action>
    Modify `arcproLab/arcpro_robot_cfg.py` to implement Decision D-05 (Robot Asset):
    - Update the robot asset path to `arcproLab/assets/robot/F1Tenth_Metric.usd`.
  </action>
  <verify>
    <automated>
      grep -q "robot_usd_path = "arcproLab/assets/robot/F1Tenth_Metric.usd"" arcproLab/arcpro_robot_cfg.py
    </automated>
  </verify>
  <done>
    `arcproLab/arcpro_robot_cfg.py` specifies `F1Tenth_Metric.usd` as the robot asset.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove Robot Stability Event</name>
  <files>arcproLab/mdp/events.py</files>
  <action>
    Modify `arcproLab/mdp/events.py` to implement Decision D-06 (Robot Stability Event):
    - Remove or comment out the `setup_robot_stability` event and any related calls or imports that are no longer necessary.
  </action>
  <verify>
    <automated>
      ! grep -q "setup_robot_stability" arcproLab/mdp/events.py
    </automated>
  </verify>
  <done>
    The `setup_robot_stability` event is no longer present or active in `arcproLab/mdp/events.py`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify True Physics Environment Setup</name>
  <what-built>
    The simulation environment has been configured with true-to-scale physics, a 1.0x metric F1Tenth robot, and a proportionally scaled OpenStreetMap track.
  </what-built>
  <how-to-verify>
    1. Run the simulation using `python arcproLab/scripts/verify_policy.py`.
    2. Observe the robot and track in the simulator.
    3. Verify that the robot appears to be 1.0x metric scale and the track lane widths are realistic (~3.5m wide).
    4. Ensure the robot spawns correctly at 0.5m above the track and settles without excessive initial collisions or bouncing.
    5. Confirm the camera view is positioned correctly relative to the robot.
    6. Ensure the simulation runs stably without unexpected physics glitches or instability issues that previously required workarounds.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the environment behaves as expected, or describe any issues encountered.
  </resume-signal>
</task>

</tasks>

<verification>
All modifications specified in the tasks are applied correctly, and the simulation runs stably with a metric-scale robot and track.
</verification>

<success_criteria>
- `arcproLab/arcpro_env_cfg.py` is updated per decisions D-01, D-02, D-03, D-07.
- `arcproLab/arcpro_robot_cfg.py` is updated per decision D-05.
- `arcproLab/mdp/events.py` is updated per decision D-06.
- The simulation can be launched and visually confirmed to be operating in a stable, true-physics metric environment as described in the objective.
</success_criteria>

<output>
After completion, create `.planning/phases/07-true-physics-revert/07-01-SUMMARY.md`
</output>