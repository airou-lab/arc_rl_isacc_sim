# RESUME HERE

## Project Context
- **Milestone:** Milestone 3: HD Perception & Production Hardening (v2.6)
- **Phase:** Phase 16: MARL Transition
- **Status:** SKRL single-agent RGB visual training is running in the background.

## Active Command
- **Background Training**: SKRL single-agent training via `start_tmux_training.sh` (cameras ON).
- **Log File**: `logs/skrl_phase1.log`
- **Background Monitor**: `strict_monitor_agent` is running on a 15-minute cron, waiting for 500k step milestones.

## Recent Changes
- Added `--enable_cameras` to `start_tmux_training.sh` to fix blind training.
- Patched stationary and GoSignal exploits. Wiped old blind checkpoints.
- Enforced RGB-only self driving directives in planning docs.

## Immediate Next Steps
- [ ] **Monitor Training**: Let the `strict_monitor_agent` handle the 500k milestone checks.
- [ ] **Visual Verification**: Use `./steer.sh` to visually verify driving behavior once the agent crosses 500k+ steps.
- [ ] **MARL Spawner**: Once single-agent SKRL is confirmed working with vision, refactor the vehicle spawner to support multiple agents per environment.
