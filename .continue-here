# RESUME HERE

## Project Context
- **Milestone:** Milestone 3: HD Perception & Production Hardening (v2.6)
- **Phase:** Phase 16: MARL Transition
- **Status:** SKRL single-agent training is running in the background.

## Active Command
- **Background Training**: SKRL single-agent training via `start_tmux_training.sh` (running `train_skrl.py`).
- **Log File**: `logs/skrl_phase1.log`
- **Target**: Ensure single-agent SKRL is working stably before proceeding to MARL.

## Recent Changes
- **SKRL Migration**: Migrated from Stable-Baselines3 to SKRL.
- **SB3 Archive**: Archived all legacy SB3 scripts and policy stack files into `archive/sb3_legacy`.
- **Infrastructure**: Modified `start_tmux_training.sh` to execute the new SKRL trainer.

## Immediate Next Steps
- [ ] **Monitor Training**: Check `logs/skrl_phase1.log` periodically to verify SKRL training is stable.
- [ ] **MARL Spawner**: Once single-agent SKRL is confirmed working, refactor the vehicle spawner to support multiple agents per environment.
- [ ] **SKRL MARL Integration**: Begin upgrading the SKRL policy stack bridge for full Phase 16 multi-agent support.
