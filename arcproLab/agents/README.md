# Agents

Contains the SKRL-based reinforcement learning definitions.

- `skrl_models.py`: Defines the Neural Network architecture (e.g., ResNet-18 vision backbone + MLPs).
- `skrl_wrappers.py`: Wraps the IsaacLab environment into an SKRL-compatible format.
